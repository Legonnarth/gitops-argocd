apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simple-app-dev
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Legonnarth/gitops-argocd
    targetRevision: main
    path: apps/simple-app/dev

  destination:
    server: https://kubernetes.default.svc
    namespace: simple-app-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
